@model string[]
@{
    ViewBag.Title = "ChildActionView";
}

<h2>ChildActionView</h2>

@foreach (var item in Model)
{
    @*@item @Html.Action("OdrediGrupuProizvoda", "ChildAction"); <br /> *@

    @Html.TextBox(@item, @item+" x") <span> je</span> @Html.Action("OdrediGrupuProizvoda", new { proizvod = item }); <br />
}